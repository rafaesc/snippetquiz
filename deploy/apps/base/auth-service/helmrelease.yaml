apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: snippetquiz-auth-service-chart
  namespace: flux-system
spec:
  interval: 5m
  url: oci://ghcr.io/rafaesc/charts/snippetquiz-auth-service-chart
  ref:
    tag: 0.0.15
  secretRef:
    name: ghcr-secret
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snippetquiz-auth-service-chart
  namespace: flux-system
spec:
  chartRef:
    kind: OCIRepository
    name: snippetquiz-auth-service-chart
    namespace: flux-system
  interval: 10m
  values:
    replicaCount: 1

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snippetquiz-api-gateway-chart
  namespace: snippetquiz
spec:
  values:
    redis:
      host: "redis-sessions.snippetquiz.svc.cluster.local"
      port: "6379"
    
    routers:
      aiContentServiceHost: "ai-content.snippetquiz.svc.cluster.local"
      authServiceHost: "auth.snippetquiz.svc.cluster.local"
      coreServiceHost: "snippetquiz-core-service-chart"

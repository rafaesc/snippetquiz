apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snippetquiz-frontend-chart
  namespace: snippetquiz
spec:
  values:
    logLevel: debug

    api:
      url: "https://sq.rafaescala.com/api/"

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: snippetquiz-frontend-chart
spec:
  chartRef:
    kind: OCIRepository
    name: snippetquiz-frontend-chart
    namespace: flux-system
  interval: 10m
  values:
    replicaCount: 1
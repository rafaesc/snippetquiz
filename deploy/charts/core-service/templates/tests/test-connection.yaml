apiVersion: v1
kind: Pod
metadata:
  name: "core-service-test-connection"
  labels:
    app: core-service
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['core-service:3003']
  restartPolicy: Never

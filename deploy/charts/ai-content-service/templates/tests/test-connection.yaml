apiVersion: v1
kind: Pod
metadata:
  name: "ai-content-service-test-connection"
  labels:
    app: ai-content-service
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['ai-content-service:3004']
  restartPolicy: Never

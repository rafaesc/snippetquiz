apiVersion: v1
kind: Pod
metadata:
  name: "auth-service-test-connection"
  labels:
    app: auth-service
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['auth-service:3002']
  restartPolicy: Never
